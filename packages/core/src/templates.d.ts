import { BrandProfile, CompileTarget, CompiledTargetOutput, PromptBrief } from './types';
export declare function buildTargetOutput(target: CompileTarget, brief: PromptBrief, profile: BrandProfile): CompiledTargetOutput;
